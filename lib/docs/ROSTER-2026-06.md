# Roster refresh — 2026-06

Driven by: deepseek-v4-pro became unusable on NVIDIA NIM (queue >1000 → 300s
`ReadTimeout`, trace-captured). Goal: swap to the second-most-capable DeepSeek and add
every free model stronger than gemma-4. Research: panel job `06bd1d23` (research:true,
kimi + nemotron converged, web-cited) + operator web verification, then **every slug
was live-probed** through the deployed gateway (16-token verifier call) before entry.

## Live probe results (2026-06-10, nvidia free endpoints)

| slug | result | action |
|---|---|---|
| `deepseek-ai/deepseek-v4-flash` | ✅ OK | **added — new primary DeepSeek** |
| `qwen/qwen3.5-397b-a17b` | ✅ OK (probe) / ⚠️ **empty content on a real 163k-token review** | added, then **DEMOTED from the default panel** (kept as a registered logical) - replaced by nemotron-ultra. Top MMLU-Pro 84.7 but unreliable on the free tier for large jobs. |
| `qwen/qwen3.5-122b-a10b` | ✅ OK (transient empty on 1st try, OK on retry) | added |
| `meta/llama-4-maverick-17b-128e-instruct` | ✅ OK | added |
| `minimaxai/minimax-m2.7` | ✅ OK (note the `minimaxai/` prefix) | added |
| `mistralai/mistral-medium-3.5-128b` | ✅ OK | added |
| `mistralai/mistral-small-4-119b-2603` | ✅ OK | added (frontier:false — mid-tier, still > gemma) |
| `mistralai/mistral-large-3` | ❌ 404 | rejected — wrong slug (real slug unconfirmed) |
| `mistralai/mistral-nemotron` | ❌ 400 Bad Request | rejected — function-calling model, needs tools payload |
| `qwen/qwen3-next-80b-a3b-instruct` | ❌ 504 / cooling on retry | rejected — unreliable on free tier |

## Removed 2026-06-11

- **`nvidia/qwen/qwen3-coder-480b-a35b-instruct` → `410 Gone`.** The dogfood `repo_audit`
  run (job `56a6d3e0`) hit it as the scheduler's first pick and got a hard 410 (model
  withdrawn from NVIDIA NIM). Removed from `providers_catalog.json`, `models_catalog.json`
  (`qwen3-coder-480b` logical), `benchmarks.json`, and `reasoning_catalog.json`. Coding
  is still covered by minimax-m2.7 + qwen3.5-397b/122b.

Rejected slugs are documented in `providers_catalog.json` (`//probe` key) so the next
refresh doesn't re-try them blindly. To re-attempt: probe via
`POST /api/panel {"panel":["nvidia/<slug>"], "max_tokens":12, "privacy":"off"}` and add
to the catalog only on a clean `ok`.

## DeepSeek decision
`deepseek-v4-pro` is **kept but demoted** (still resolvable as the `deepseek-v4-pro`
logical; removed from the default panel and `frontier:false` in benchmarks).
`deepseek-v4-flash` (284B MoE distillation, ~5-8% below pro, reliably served) is the new
default DeepSeek with a github-hosted deepseek-v3 failover. `deepseek-r1` and
`deepseek-v3.2` on NVIDIA are "Free Endpoint **Deprecated**" — not added.

## Catalog changes
- `providers_catalog.json`: 6 new nvidia slugs + deepseek-v4-flash.
- `models_catalog.json`: new logicals with conservative `ctx` (256K→262144, Mistral→131072).
- `benchmarks.json`: dated, indicative entries (frontier flags) for each — keeps
  `/api/roster` + the ≥2-frontier guarantee honest.
- `panel.json`: default judges now kimi-k2.6 / deepseek-v4-flash / qwen3.5-397b / glm-5.1.

ctx values are conservative (understated vs. advertised) so repo-pack budgeting never
overflows a real window; raise them only after testing large contexts per host.
