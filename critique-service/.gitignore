# never commit real secrets
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# local metrics cache (durable copy lives in the HF Dataset, not the repo)
data/

# materialized client output (run artifacts / panel files)
runs/
panel_out/
